run:
  - exec: echo "Enabling IPv6 listener"

  - file:
      path: "/etc/nginx/conf.d/outlets/http/ipv6.conf"
      contents: |
        listen [::]:80;

  - file:
      path: "/etc/nginx/conf.d/outlets/https/ipv6.conf"
      contents: |
        listen [::]:443 ssl http2;
