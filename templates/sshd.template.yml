run:
  - exec: echo "The sshd.template.yml file is deprecated; you can remove it from your containers/app.yml file."
  - exec: "[[ \"$(date +'%Y-%m-%d')\" > '2020-09-01' ]] || (echo '### The deprecation period for sshd.template.yml has ended, remove it from your container configuration. ###'; exit 1)"
